
const hello = (name: string ) => console.log(`hello ${name}`);
const topla = (a: number , b: number) => a+b;
const cikar = (a: number , b: number) => a-b;

export const title_ = "test";

export const user = {
    name: "mehmet", 
    surname: "seven"
};

export {hello,topla,cikar};

